@model IEnumerable<Cs390914_SD_3.Models.Workout>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Clients.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrainerTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrainerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clients.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrainerTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrainerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkoutID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkoutID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkoutID })
        </td>
    </tr>
}

</table>
